<section class="hero">
  <div class="container hero-content">
    <div class="hero-text">
      <p class="tagline">NYC · Drinking Water Dashboard</p>
      <h1>See Borough Water Quality at a Glance</h1>
      <p class="hero-subtitle">
        Explore drinking water samples across all five boroughs, compare
        quality indicators, and spot potential issues early.
      </p>

      <div class="hero-actions">
        <a href="/boroughs" class="btn btn-primary">
          View Borough Overview
        </a>
        <a href="/waterSamples" class="btn btn-outline">
          Browse Water Samples
        </a>
      </div>

      <ul class="hero-highlights">
        <li><span class="stat-label">5</span> boroughs covered</li>
        <li><span class="stat-label">1000+</span> water samples tracked</li>
        <li><span class="stat-label">Updated</span> with latest records</li>
      </ul>
      <p class="disclaimer">* Demo data for academic use only</p>
    </div>

    <aside class="hero-card">
      <h2>This Week's Best Borough</h2>
      <ul class="snapshot-list">
        {{#each boroughSnapshots}}
          <li><span>{{this.name}}</span><strong>{{this.voteCount}} votes</strong></li>
        {{/each}}
      </ul>
      <p class="card-note">
        Vote counts this week. Cast your vote to support your favorite borough!
      </p>
      {{#if isAuthenticated}}
        <p>
          <a href="/votes/best" class="btn btn-primary">Vote</a>
        </p>
      {{else}}
        <p>
          <a href="/users/login?redirect=/votes/best" class="btn btn-outline">Sign in to vote</a>
        </p>
      {{/if}}
    </aside>
  </div>
</section>

<section class="info-section">
  <div class="container info-grid">
    <a class="info-item">
      <h3>Track Borough Trends</h3>
      <p>
        View summarized borough-level descriptions and understand how water
        quality changes over time.
      </p>
    </a>
    <a class="info-item" >
      <h3>Explore Sample Details</h3>
      <p>
        Drill into individual water samples, including collection date,
        borough, and quality indicators.
      </p>
    </a>

    {{#if currentUser}}
      <a class="info-item" >
        <h3>Create a Profile</h3>
        <p>Signed in as <strong>{{currentUser.username}}</strong>. Manage your profile and favorites.</p>
      </a>
    {{else}}
      <a class="info-item" >
        <h3>Create a Profile</h3>
        <p>Sign up to save favorite boroughs, leave comments, and participate in community discussions.</p>
      </a>
    {{/if}}
  </div>
</section>
